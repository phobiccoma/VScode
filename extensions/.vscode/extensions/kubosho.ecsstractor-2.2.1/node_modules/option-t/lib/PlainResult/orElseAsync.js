"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.orElseAsyncForResult = void 0;
var assert_1 = require("../internal/assert");
var ErrorMessage_1 = require("../internal/ErrorMessage");
var Result_1 = require("./Result");
var unwrap_1 = require("./unwrap");
/**
 *  Calls _recoverer_ and return its returned value if the result is `Err(E)`,
 *  otherwise returns the `Ok(T)` value of self.
 */
function orElseAsyncForResult(input, recoverer) {
    if (Result_1.isOk(input)) {
        return Promise.resolve(input);
    }
    var inner = unwrap_1.unwrapErrFromResult(input);
    var defaultValue = recoverer(inner);
    // If this is async function, this always return Promise, but not.
    // We should check to clarify the error case if user call this function from plain js
    // and they mistake to use this.
    assert_1.assertIsPromise(defaultValue, ErrorMessage_1.ERR_MSG_RECOVERER_MUST_RETURN_PROMISE);
    return defaultValue;
}
exports.orElseAsyncForResult = orElseAsyncForResult;
