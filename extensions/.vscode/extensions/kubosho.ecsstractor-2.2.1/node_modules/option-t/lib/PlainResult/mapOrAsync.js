"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mapOrAsyncForResult = void 0;
var assert_1 = require("../internal/assert");
var ErrorMessage_1 = require("../internal/ErrorMessage");
var Result_1 = require("./Result");
var unwrap_1 = require("./unwrap");
/**
 *  Return the result of _transformer_ with using _input_ as an argument for it if _input_ is `Ok(T)`.
 *  Otherwise, return _defaultValue_.
 *
 *  Basically, this operation is a combination `mapAsync()` and `unwrapOr()`.
 */
function mapOrAsyncForResult(input, defaultValue, transformer) {
    if (Result_1.isErr(input)) {
        var result_1 = Promise.resolve(defaultValue);
        return result_1;
    }
    var source = unwrap_1.unwrapFromResult(input);
    var result = transformer(source);
    // If this is async function, this always return Promise, but not.
    // We should check to clarify the error case if user call this function from plain js
    // and they mistake to use this.
    assert_1.assertIsPromise(result, ErrorMessage_1.ERR_MSG_TRANSFORMER_MUST_RETURN_PROMISE);
    return result;
}
exports.mapOrAsyncForResult = mapOrAsyncForResult;
