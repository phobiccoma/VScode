import { expectNotNullAndUndefined } from "./expect.mjs";
import { ERR_MSG_UNWRAP_NO_VAL_FOR_MAYBE } from "./ErrorMessage.mjs";
/**
 *  Return _value_ as `T` if the passed _value_ is not `null` and `undefined`.
 *  Otherwise, throw `TypeError`.
 */

export function unwrapMaybe(value) {
  return expectNotNullAndUndefined(value, ERR_MSG_UNWRAP_NO_VAL_FOR_MAYBE);
}