import { expectNotNull } from "./expect.mjs";
import { ERR_MSG_DEFAULT_VALUE_MUST_NOT_BE_NO_VAL_FOR_NULLABLE } from "./ErrorMessage.mjs";
/**
 *  Return _input_ as `T` if the passed _input_ is not `null`.
 *  Otherwise, return _defaultValue_.
 *
 *  * _defaultValue_ must not be `Nullable<*>`.
 *  * If the _defaultValue_ is `null`, throw `TypeError`.
 */

export function unwrapOrFromNullable(input, defaultValue) {
  if (input !== null) {
    return input;
  }

  var passed = expectNotNull(defaultValue, ERR_MSG_DEFAULT_VALUE_MUST_NOT_BE_NO_VAL_FOR_NULLABLE);
  return passed;
}