import { isNotNull } from "./Nullable.mjs";
/**
 *  Return _b_ if _a_ is not `null`.
 *  Otherwise, return _a_.
 */

export function andForNullable(a, b) {
  if (isNotNull(a)) {
    return b;
  }

  return a;
}