import { createErr, createOk } from "../PlainResult/Result.mjs";
/**
 * Transforms the `Option<T>` into a `Result<T, E>`, mapping `Some(v)` to `Ok(v)` and `None` to `Err(err())`.
 */

export function okOrElseForPlainOption(input, recoverer) {
  if (input.ok) {
    var v_1 = createOk(input.val);
    return v_1;
  }

  var e = recoverer();
  var v = createErr(e);
  return v;
}