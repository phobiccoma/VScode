import { expectIsSome } from "./expect.mjs";
/**
 *  Return the inner `T` of a `Some(T)`.
 *
 *  @throws {TypeError}
 *      Throws if the self is a `None`.
 */

export function unwrapOption(input) {
  return expectIsSome(input, 'called with `None`');
}