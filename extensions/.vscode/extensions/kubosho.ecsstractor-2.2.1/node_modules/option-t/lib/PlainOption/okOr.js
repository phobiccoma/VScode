"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.okOrForPlainOption = void 0;
var Result_1 = require("../PlainResult/Result");
/**
 * Transforms the `Option<T>` into a `Result<T, E>`,
 * mapping `Some(v)` to `Ok(v)` and None to Err(err).
 *
 * Arguments passed to this are eagerly evaluated;
 * if you are passing the result of a function call, it is recommended to use okOrElse, which is lazily evaluated.
 */
function okOrForPlainOption(input, err) {
    if (input.ok) {
        var v = Result_1.createOk(input.val);
        return v;
    }
    var e = Result_1.createErr(err);
    return e;
}
exports.okOrForPlainOption = okOrForPlainOption;
