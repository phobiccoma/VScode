import { andThenForOption } from "./andThen.mjs";

function flatten(input) {
  return input;
}
/**
 *  Flatten the inner value of _input_.
 */


export function flattenForOption(input) {
  var result = andThenForOption(input, flatten);
  return result;
}