/**
 *  Return _input_ as `T` if the passed _input_ is `Some(T)`.
 *  Otherwise, return the result of _recoverer_.
 */
export function orElseForOption(input, recoverer) {
  if (input.ok) {
    return input;
  }

  var fallback = recoverer();
  return fallback;
}