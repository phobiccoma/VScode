import { createSome, createNone, isSome } from "../PlainOption/Option.mjs";
import { unwrapOption } from "../PlainOption/unwrap.mjs";
import { createClassicNone, createClassicSome } from "./ClassicOption.mjs";
export function compatToPlainOption(classic) {
  if (classic.isSome) {
    var val = classic.unwrap();
    var result_1 = createSome(val);
    return result_1;
  }

  var result = createNone();
  return result;
}
export function compatToClassicOption(plain) {
  if (isSome(plain)) {
    var val = unwrapOption(plain);
    var result_2 = createClassicSome(val);
    return result_2;
  }

  var result = createClassicNone();
  return result;
}