import { isNotUndefined } from "./Undefinable.mjs";
/**
 *  Return one of this pattern:
 *
 *  1. a=others, b=undefined => _a_
 *  2. a=undefined, b=others => _b_
 *  3. Others => `undefined`
 */

export function xorForUndefinable(a, b) {
  var aIsSome = isNotUndefined(a);
  var bIsSome = isNotUndefined(b);

  if (aIsSome && !bIsSome) {
    return a;
  }

  if (!aIsSome && bIsSome) {
    return b;
  }

  return undefined;
}