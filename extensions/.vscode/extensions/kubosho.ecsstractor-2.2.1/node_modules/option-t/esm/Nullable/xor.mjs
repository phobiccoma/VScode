import { isNull } from "./Nullable.mjs";
/**
 *  Return one of this pattern:
 *
 *  1. a=others, b=null => _a_
 *  2. a=null, b=others => _b_
 *  3. Others => `null`
 */

export function xorForNullable(a, b) {
  var aIsNull = isNull(a);
  var bIsNull = isNull(b);

  if (!aIsNull && bIsNull) {
    return a;
  } else if (aIsNull && !bIsNull) {
    return b;
  } else {
    return null;
  }
}