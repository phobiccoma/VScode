/**
 *  Return _input_ as `T` if the passed _input_ is not `null`.
 *  Otherwise, return the result of _recoverer_.
 */
export function orElseForNullable(input, recoverer) {
  if (input !== null) {
    return input;
  }

  var fallback = recoverer();
  return fallback;
}