import { createNone, createSome } from "../PlainOption/Option.mjs";
/**
 *  Convert to `Some(T)` if _input_ is `Ok(T)`.
 *  Otherwise, return `None`.
 */

export function toOptionFromOk(input) {
  if (input.ok) {
    return createSome(input.val);
  }

  return createNone();
}
/**
 *  Convert to `Some(E)` if _input_ is `Err(E)`.
 *  Otherwise, return `None`.
 */

export function toOptionFromErr(input) {
  if (!input.ok) {
    return createSome(input.err);
  }

  return createNone();
}