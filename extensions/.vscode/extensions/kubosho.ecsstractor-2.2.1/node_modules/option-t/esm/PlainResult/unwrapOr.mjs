/**
 *  Unwraps a result _input_, returns the content of an `Ok(T)`.
 *  If the value is an `Err(E)` then return _defaultValue_.
 */
export function unwrapOrFromResult(input, defaultValue) {
  if (input.ok) {
    var val = input.val;
    return val;
  }

  return defaultValue;
}