import { andThenForResult } from "./andThen.mjs";

function flatten(input) {
  return input;
}
/**
 * Converts from `Result<Result<T, E>, E>` to `Result<T, E>`
 */


export function flattenForResult(input) {
  var result = andThenForResult(input, flatten);
  return result;
}