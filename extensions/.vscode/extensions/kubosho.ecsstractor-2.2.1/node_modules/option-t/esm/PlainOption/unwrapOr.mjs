/**
 *  Unwraps a result _input_, returns the content of an `Some(T)`.
 *  If the value is an `None` then return _defaultValue_.
 */
export function unwrapOrFromOption(input, defaultValue) {
  if (input.ok) {
    var val = input.val;
    return val;
  }

  return defaultValue;
}