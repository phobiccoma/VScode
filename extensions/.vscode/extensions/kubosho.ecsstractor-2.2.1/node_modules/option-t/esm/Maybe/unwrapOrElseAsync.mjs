import { assertIsPromise } from "../internal/assert.mjs";
import { ERR_MSG_RECOVERER_MUST_RETURN_PROMISE } from "../internal/ErrorMessage.mjs";
import { isNotNullAndUndefined } from "./Maybe.mjs";
import { expectNotNullAndUndefined } from "./expect.mjs";
import { ERR_MSG_RECOVERER_MUST_NOT_RETURN_NO_VAL_FOR_MAYBE } from "./ErrorMessage.mjs";

function check(value) {
  var passed = expectNotNullAndUndefined(value, ERR_MSG_RECOVERER_MUST_NOT_RETURN_NO_VAL_FOR_MAYBE);
  return passed;
}
/**
 *  Return _input_ as `T` if the passed _input_ is not `null` and `undefined`.
 *  Otherwise, return the result of _recoverer_.
 *
 *  * The result of _recoverer_ must not be `Maybe<*>`.
 *  * If the result of _recoverer_ is `null` or `undefined`, throw `TypeError`.
 */


export function unwrapOrElseAsyncFromMaybe(input, recoverer) {
  if (isNotNullAndUndefined(input)) {
    return Promise.resolve(input);
  }

  var fallback = recoverer(); // If this is async function, this always return Promise, but not.
  // We should check to clarify the error case if user call this function from plain js
  // and they mistake to use this.

  assertIsPromise(fallback, ERR_MSG_RECOVERER_MUST_RETURN_PROMISE);
  var passed = fallback.then(check);
  return passed;
}