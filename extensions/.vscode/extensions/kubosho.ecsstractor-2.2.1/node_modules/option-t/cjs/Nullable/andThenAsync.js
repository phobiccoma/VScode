"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.andThenAsyncForNullable = void 0;
var assert_1 = require("../internal/assert");
var ErrorMessage_1 = require("../internal/ErrorMessage");
var Nullable_1 = require("./Nullable");
/**
 *  Returns `null` if the _input_ is `null`,
 *  otherwise calls _transformer_ with the value and returns the result.
 *
 *  XXX:
 *  Some languages call this operation flatmap.
 *  But we don't provide `flatMap()` as alias of this function.
 *  because it's too hard to undarstand that "flatMap" operation for `T | null`
 */
function andThenAsyncForNullable(input, transformer) {
    if (Nullable_1.isNull(input)) {
        return Promise.resolve(input);
    }
    var result = transformer(input);
    // If this is async function, this always return Promise, but not.
    // We should check to clarify the error case if user call this function from plain js
    // and they mistake to use this.
    assert_1.assertIsPromise(result, ErrorMessage_1.ERR_MSG_TRANSFORMER_MUST_RETURN_PROMISE);
    return result;
}
exports.andThenAsyncForNullable = andThenAsyncForNullable;
