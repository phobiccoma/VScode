"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mapErrForResult = void 0;
var Result_1 = require("./Result");
/**
 *  Maps a `Result<T, E>` to `Result<T, F>` by applying a _transformer_ function `mapFn<E, F>`
 *  to an contained `Err(E)` value, leaving an `Ok(T)` value untouched.
 *
 *  This function can be used to pass through a successful result while handling an error.
 */
function mapErrForResult(input, transformer) {
    if (input.ok) {
        return input;
    }
    var result = transformer(input.err);
    return Result_1.createErr(result);
}
exports.mapErrForResult = mapErrForResult;
