"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mapOrForResult = void 0;
/**
 *  Return the result of _transformer_ with using _input_ as an argument for it if _input_ is `Ok(T)`.
 *  Otherwise, return _defaultValue_.
 *
 *  Basically, this operation is a combination `map()` and `unwrapOr()`.
 */
function mapOrForResult(input, defaultValue, transformer) {
    if (input.ok) {
        var result = transformer(input.val);
        return result;
    }
    return defaultValue;
}
exports.mapOrForResult = mapOrForResult;
