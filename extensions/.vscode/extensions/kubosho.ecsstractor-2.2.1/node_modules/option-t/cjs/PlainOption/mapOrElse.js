"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mapOrElseForOption = void 0;
/**
 *  Maps a `Option<T>` to `U` by applying _transformer_ to a contained `Some(T)` value in _input_,
 *  or a _recoverer_ function to a contained `None` value in _input_.
 *  This function can be used to unpack a successful result while handling an error.
 */
function mapOrElseForOption(input, recoverer, transformer) {
    if (!input.ok) {
        var fallback = recoverer();
        return fallback;
    }
    var result = transformer(input.val);
    return result;
}
exports.mapOrElseForOption = mapOrElseForOption;
