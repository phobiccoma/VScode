"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mapForOption = void 0;
var Option_1 = require("./Option");
/**
 *  Maps a `Option<T>` to `Option<U>` by applying a _transformer_ function
 *  to an contained `Some(T)` value, leaving an `None` value untouched.
 *
 *  This function can be used to compose the results of two functions.
 */
function mapForOption(input, transformer) {
    if (!input.ok) {
        return input;
    }
    var result = transformer(input.val);
    return Option_1.createSome(result);
}
exports.mapForOption = mapForOption;
